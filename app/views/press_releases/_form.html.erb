<%= form_for(press_release) do |f| %>
  <% if press_release.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(press_release.errors.count, "error") %> prohibited this press_release from being saved:</h2>

      <ul>
      <% press_release.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :headline, 'Titular' %>
    <%= f.text_field :headline %>
  </div>

  <div class="field">
    <%= f.label :subheadline, 'Bajada de título' %>
    <%= f.text_area :subheadline, :maxlength => 250 %>
  </div>

  <div class="field">
    <%= f.label :body, 'Cuerpo' %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit("Publicar noticia") %>
  </div>
<% end %>
